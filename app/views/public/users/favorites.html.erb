<div class="container my-3">
  <div class="row text-center">
    <% if @favorite_items.present? %>
      <%= render "public/items/index", items: @favorite_items %>
    <% else %>
      <h3 class="col text-center">いいねした商品がありません。</h3>
    <% end %>
  </div>
</div>
