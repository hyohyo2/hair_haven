<div class="text-center my-5">
  <% @tags.each do |tag| %>
    <span class="checkbox-item">
      <%= link_to item_index_path(tag) do %>
        <%= tag.name %>
      <% end %>
    </span>
  <% end %>
</div>
